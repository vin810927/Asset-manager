# Agent Rules

## Language

- 所有回覆使用繁體中文。
- 不要使用簡體中文。
- 技術名詞可保留英文，例如 React、Vite、Supabase、localStorage、pull request。

## Project Context

這是一個個人資產管理與理財輔助工具。

目標是讓使用者定期匯入資產數值，彙整股票、現金、基金、貸款、共同支出與理財目標，未來可加入 AI 輔助建議。

目前階段是 starter project，優先目標是讓資料輸入流程簡潔、資料結構清楚、容易部署到 Vercel。

## Safety

- 不要儲存密碼、API key、token。
- 不要自動下單、轉帳、購買金融商品。
- 投資相關內容只能作為研究、整理與風險提示，不可給出保證性建議。
- 不要建立任何會直接連接券商並執行交易的功能。
- 涉及醫療或院內資料時，必須去識別化，不可保存病人個資。
- 不要把環境變數寫入前端程式碼。

## Workflow

修改前：

1. 閱讀 `README.md`
2. 閱讀 `TODO.md`
3. 閱讀 `decisions.md`
4. 閱讀本檔案 `AGENTS.md`
5. 簡短說明實作計畫

修改時：

- 每次修改盡量保持小範圍。
- 優先維持可執行狀態。
- 不要一次重寫整個專案，除非使用者明確要求。
- 不要引入大型套件，除非有明確理由。
- 不要修改與任務無關的檔案。

修改後：

1. 執行可用的 lint、test、build
2. 回報修改摘要
3. 回報修改檔案
4. 回報測試結果
5. 回報下一步建議
6. 若有重要設計決策，更新 `decisions.md`
7. 若完成或新增工作項目，更新 `TODO.md`

## Code Style

- 使用 React function components
- 使用 hooks 管理狀態
- 優先使用清楚命名，不過度抽象
- 資料處理邏輯可抽到 `src/utils.js`
- UI 儘量精簡，減少捲動
- 表單欄位應根據資產類型動態顯示
- 股票只輸入代號，不要求輸入名稱
- 同股票代號應合併總覽，但保留分次購入明細
- 貸款需包含本金、年限、利率、起始日期

## Response Format

完成任務後，請用以下格式回覆：

```text
修改摘要：
- ...

修改檔案：
- ...

測試結果：
- ...

下一步建議：
- ...
```
